[package]
name = "fastest"
version = "0.1.0"
edition = "2021"

[lib]
name = "fastest"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20.0", features = ["extension-module"] }

[package.metadata.maturin]
name = "_fastest"
